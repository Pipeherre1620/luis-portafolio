<section id="about">
	<div class="about-label">
		<p>Sobre mí</p>
	</div>

	<div class="about-content">
		<h2>
			Construyo sistemas de software pensando en cómo
			pueden evolucionar con el tiempo.
		</h2>

		<p>
			Construyo sistemas de software con enfoque en desarrollo
			backend, cloud y arquitectura de software.
		</p>

		<p>
			Me gusta entender cómo funcionan los sistemas, diseñar
			soluciones que puedan evolucionar con el tiempo y convertir
			problemas técnicos complejos en software simple y mantenible.
		</p>

		<p>
			Me interesan especialmente los sistemas distribuidos,
			las arquitecturas cloud-native y el liderazgo técnico.
		</p>

		<div class="about-skills">
			<span>Backend Engineering</span>
			<span>Cloud & AWS</span>
			<span>Software Architecture</span>
			<span>Technical Leadership</span>
		</div>
	</div>
</section>

<style>
	#about {
		display: grid;
		grid-template-columns: 1fr 2fr;
		gap: 80px;

		padding: 160px 48px;

		border-top: 1px solid var(--border);
	}

	.about-label {
		margin: 0;

		font-size: var(--text-xs);
		font-weight: var(--weight-medium);

		letter-spacing: 0.12em;
		text-transform: uppercase;

		color: var(--accent);
	}

	.about-content h2 {
		max-width: 900px;

		margin: 0;

		font-size: clamp(2rem, 4vw, 4rem);
		line-height: 1.05;

		font-weight: var(--weight-regular);
		letter-spacing: -0.03em;
	}

	.about-content > p {
		max-width: 700px;

		margin-top: 32px;

		font-size: 1.1rem;
		line-height: 1.7;

		color: var(--muted);
	}

	.about-skills {
		display: flex;
		flex-wrap: wrap;
		gap: 12px;

		margin-top: 48px;
	}

	.about-skills span {
		padding: 10px 16px;

		border: 1px solid var(--border);
		border-radius: 999px;

		font-size: 0.85rem;

		transition:
			color 0.25s ease,
			border-color 0.25s ease,
			background 0.25s ease;
	}

	.about-skills span:hover {
		color: var(--accent);
		border-color: var(--accent);
		background: rgba(125, 211, 252, 0.05);
	}

	@media (max-width: 768px) {
		#about {
			grid-template-columns: 1fr;
			gap: 48px;

			padding: 100px 24px;
		}

		.about-content h2 {
			font-size: clamp(2rem, 10vw, 3rem);
		}
	}
</style>