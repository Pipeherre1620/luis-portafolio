<section id="architecture">
	<div class="architecture-header">
		<p>Arquitectura</p>

		<h2>
			No solo escribo código.
			<br />
			Pienso en cómo funciona
			<br />
			el sistema completo.
		</h2>
	</div>

	<div class="architecture-grid">
		<article>
			<span>01</span>

			<div>
				<h3>Diseño de Sistemas</h3>
				<p>
					Diseñar sistemas pensando en evolución,
					mantenibilidad, resiliencia y escalabilidad.
				</p>
			</div>
		</article>

		<article>
			<span>02</span>

			<div>
				<h3>Sistemas Distribuidos</h3>
				<p>
					Trabajar con comunicación asíncrona, eventos,
					consistencia y desacoplamiento entre servicios.
				</p>
			</div>
		</article>

		<article>
			<span>03</span>

			<div>
				<h3>Cloud</h3>
				<p>
					Construir soluciones aprovechando servicios
					cloud-native y principios de arquitectura
					modernos.
				</p>
			</div>
		</article>

		<article>
			<span>04</span>

			<div>
				<h3>Decisiones Técnicas</h3>
				<p>
					Analizar trade-offs, documentar decisiones
					y elegir soluciones pensando en el contexto
					del sistema.
				</p>
			</div>
		</article>
	</div>
</section>

<style>
	#architecture {
		padding: 180px 48px;
		border-top: 1px solid rgba(255, 255, 255, 0.15);
	}

	.architecture-header {
		max-width: 1200px;
		margin: 0 auto 120px;
	}

	.architecture-header > p {
		margin: 0 0 48px;
		font-size: 0.85rem;
		text-transform: uppercase;
		letter-spacing: 0.15em;
		opacity: 0.5;
	}

	.architecture-header h2 {
		max-width: 1000px;
		margin: 0;
		font-size: clamp(3rem, 7vw, 7rem);
		line-height: 0.95;
		font-weight: 400;
		letter-spacing: -0.05em;
	}

	.architecture-grid {
		max-width: 1200px;
		margin: 0 auto;
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 1px;
		background: rgba(255, 255, 255, 0.1);
	}

	.architecture-grid article {
		padding: 48px;
		background: var(--background);
		min-height: 280px;
		display: flex;
		flex-direction: column;
		justify-content: space-between;
		transition: transform 0.3s ease;
	}

	.architecture-grid article:hover {
		transform: translateY(-4px);
	}

	.architecture-grid span {
		font-size: 0.85rem;
		opacity: 0.5;
	}

	.architecture-grid h3 {
		margin: 60px 0 0;
		font-size: 1.5rem;
		font-weight: 400;
	}

	.architecture-grid p {
		max-width: 500px;
		margin: 16px 0 0;
		line-height: 1.7;
		opacity: 0.6;
	}

    @media (max-width: 768px) {
        #architecture {
            padding: 100px 24px;
        }

        .architecture-header {
            margin-bottom: 80px;
        }

        .architecture-header h2 {
            font-size: clamp(2.5rem, 11vw, 4rem);
        }

        .architecture-grid {
            grid-template-columns: 1fr;
        }

        .architecture-grid article {
            min-height: 240px;
            padding: 32px;
        }
    }

</style>

